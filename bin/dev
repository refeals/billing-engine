#!/usr/bin/env bash
# Starts the Rails API (port 3001) and the Vite dev server (port 3000) together.
# Ctrl+C stops both.
set -euo pipefail

root="$(cd "$(dirname "$0")/.." && pwd)"

trap 'kill 0' EXIT

(cd "$root/api" && bin/rails server) &
(cd "$root/web" && pnpm dev) &

wait
