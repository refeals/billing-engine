# Demo data is built in plan 12 (docs/12-scenario-lab-and-seeds.md) through the real
# services and the simulated clock, never by inserting rows directly, so the audit trail
# and the provider event log stay coherent from the first run.
